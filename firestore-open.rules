rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORAL: Acceso completo mientras solucionamos el problema de autenticación
    // ESTO ES INSEGURO - SOLO PARA DESARROLLO/DEBUGGING
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
