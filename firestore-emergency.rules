rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EMERGENCY: Permitir acceso temporal mientras solucionamos el problema
    // ESTO ES TEMPORAL - REVERTIR DESPUÉS DE SOLUCIONAR
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
